# Git hooks configuration
pre-commit:
  # Run lint and format checks in parallel
  parallel: true
  commands:
    lint:
      run: bun run lint
    format:
      run: bun run format:check

# Pre-commit checks
pre-push:
  parallel: true
  commands:
    test:
      run: bun test
    build:
      run: bun run build

